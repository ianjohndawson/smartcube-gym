{
  "name": "block-trainer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Block-building trainer for Rubik's cube methods (Petrus, Roux, LEOR, APB) with GAN smart cube support",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "gen": "node scripts/gen-journeys.ts",
    "deploy": "gh-pages -d dist"
  },
  "dependencies": {
    "gan-web-bluetooth": "^3.0.2",
    "rxjs": "^7.8.1"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
